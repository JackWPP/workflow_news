{
  "app_dir": ".",
  "build_command": "pip install -r requirements.txt",
  "start_command": "python -m uvicorn main:app --host 0.0.0.0 --port ${PORT:-8765}",
  "install_command": "pip install -r requirements.txt",
  "python_version": "3.11"
}
